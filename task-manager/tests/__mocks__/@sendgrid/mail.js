/*
    sgMail.setApiKey(process.env.SENDGRID_API_KEY);

    sgMail.send({
        to: 'yiyinghahalin@gmail.com',
        from: 'yiyinghahalin@gmail.com',
        subject: 'This is my sg mail test',
        text: 'Hope it works!'
    });
*/

// Since no returned data is needed in src/email/accounts.js for these functions,
// we have to do nothing about it.
module.exports = {
    setApiKey() {

    },
    send() {

    }
}